{
  "name": "civic-issue-management-system",
  "version": "1.0.0",
  "description": "Civic Issue Management System using PERN stack and Supabase",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev:server": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "db:init": "npm run db:init --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
